# העתיקו קובץ זה ל-.env באותה תיקייה, ומלאו את הפרטים האמיתיים.
# קובץ ה-.env עצמו לא נמצא בגיט בכוונה (מכיל סודות).

# כתובת חיבור למסד הנתונים PostgreSQL
DATABASE_URL=postgresql://USERNAME:PASSWORD@localhost:5432/DATABASE_NAME

# הפורט שעליו ירוץ השרת
PORT=5000
